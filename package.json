{
  "name": "koco-string-utilities",
  "version": "1.3.1",
  "homepage": "https://github.com/cbcrc/koco-string-utilities",
  "authors": [
    "fcharest <frederik.charest@radio-canada.ca>"
  ],
  "main": "src/string-utilities.js",
  "description": "String utilities to be used with a koco project",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "keywords": [
    "string",
    "utilities",
    "koco"
  ],
  "license": "MIT",
  "dependencies": {
    "slug": "^0.9.1"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
